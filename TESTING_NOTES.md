# TESTING NOTES

## 静态检查

- `node --check dist/app.js` 通过。
- 用 Node VM + DOM stub 执行核心计算代码，验证运行时模型函数无语法/初始化错误。

## 当前模拟数据的可复现结果

使用固定随机种子：

- 240 条样本；160 development / 80 holdout。
- 基础模型不学习 alpha；缺少所需能力时统一按 `0.25` 计算。
- 调整后逐题 alpha 后验均值约：
  - Q1 `0.466702`
  - Q2 `0.438585`
  - Q3 `0.472793`
  - Q4 `0.299069`
  - Q5 `0.128383`
  - Q6 `0.524493`
  - Q7 `0.341215`
  - Q8 `0.419424`
  - Q9 `0.183799`

Holdout：

- 独立集基础模型 NLL 约 `0.461017`
- 独立集逐题 alpha 模型 NLL 约 `0.392850`
- 独立集基础模型 AUC 约 `0.895804`
- 独立集逐题 alpha 模型 AUC 约 `0.902056`
- 独立集 50% 阈值一致率约 `80.0% → 84.6%`（240 个“人 × 能力”判断中多 11 个与参考标签一致）
- 50% threshold agreement 两者均约 `81.7%`

默认复盘样本为 P019 的逻辑判断：参考标签为“具备”，基础模型约 `18.2%`，逐题 alpha 模型约 `57.2%`，方向与参考一致并跨过 50% 判断线。

## 诊断 smoke checks

- 合成数据检查使用固定种子，基础模型在能力已知、完全按 90% / 25% 规则生成的模拟数据上，50% 阈值下一致率约 97.2%。
- 真实开发数据逐题预测/实际对比同时包含向上和向下偏差，避免把玩家直接推向“把 25% 统一调高”这种单一结论。
- 玩家必须依次完成：ground-truth 用途判断 → 25% 四选一判断 → toy inference → raw-data reading → first-result reading → inference debugging → per-question diagnosis → model change choice，才会解锁最终模型。

## UI 检查要点

- 10 段流程在桌面显示完整进度；窄屏进度条横向滚动。
- 160 × 9 答题矩阵可横向滚动，格子带 tooltip。
- 能力矩阵采用 3 列紧凑 heatmap；参考标签和概率矩阵视觉编码不同。
- 逐题 predicted-vs-actual 使用成对横条，避免只给抽象 fit score。
- 逐题 alpha 同时显示后验均值和近似 50% 不确定区间。
- holdout 在前 8 步保持视觉锁定；模型冻结后才显示具体记录。

## 成绩与留言功能

- 9 个关键交互分别记录第一次选择；返回旧步骤重选不会覆盖第一次选择。
- 计分：首次正确 `f=100`，反馈后修正 `r=60`，9 项平均后四舍五入。
- 前端只生成 GitHub Issue 草稿，不持有 GitHub Token，也不代玩家发布。
- GitHub Actions 重新根据 `marks` 计算分数，不采信 Issue 文本中手写的分数。
- 同账号保留最高公开成绩；最近一条公开留言进入留言区。
- 关闭 Issue 或添加 `community-hidden` 标签会让记录在下一次快照更新时退出公开区。
