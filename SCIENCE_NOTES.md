# SCIENCE NOTES

## 与《Model-Based Machine Learning》第 2 章的对应

这一版保留了该章的核心方法顺序：

1. 用明确假设描述“技能如何产生答题”。
2. 在简单例子上先测试模型行为。
3. 查看真实数据并运行第一次推断。
4. 用开发期 ground truth 检查技能推断。
5. 把失败来源拆成 data / model / inference。
6. 用模型自己生成的合成数据检查 inference。
7. 固定参考技能后比较 model-predicted responses 与 observed responses，以定位错误假设。
8. 把 guess probability 变成可学习参数，并允许不同题目有不同值。
9. 用多个评价指标检查改进。

项目额外加入了独立 holdout，以满足“先开发、最后独立比较”的实验设计要求。

## 变量与假设

每个样本有 3 个二元潜在能力 `skill_k` 和 9 个二元观测 `isCorrect_j`。

题目技能需求 `req_jk` 是已知标注。

- `P(skill_k = 1) = 0.5`
- 如果一个人具备题目 `j` 要求的全部能力：`P(isCorrect_j=1)=0.9`
- 基础模型中，如果缺少题目所需能力：`P(isCorrect_j=1)=0.25`
- 调整后的模型中，如果缺少题目所需能力：`P(isCorrect_j=1)=alpha_j`

基础模型把 25% 作为四选一随机猜中的直接规则；只有在诊断显示这条规则过于简单之后，才引入可学习的逐题 `alpha_j`。

## 为什么基础模型先用 0.25

每道题有四个选项、一个正确答案。完全不知道答案并等概率随机选择时，答对概率为 `1/4 = 0.25`。

真实答题并不等于完全随机猜：候选人可以排除选项、依赖一般经验或利用题干线索。这正是逐题 guess probability 可能不同的原因。

## 逐题 alpha 的先验

只有调整后的每个 `alpha_j` 使用 `Beta(2.5, 7.5)` 作为起点：

- 先验均值 `2.5 / (2.5 + 7.5) = 0.25`
- 分布仍然给更高或更低的 guess probability 留出空间。

## 学习方式

为了保持“ground truth 只用于评价”的原则，逐题 `alpha_j` 的学习不读取自评技能。

实现使用一个小型 EM/迭代贝叶斯更新：

1. 给定当前 9 个 `alpha_j`，枚举 3 个二元能力的 8 种组合，得到每个人的能力后验。
2. 对每道题，计算“该人缺少所需能力”的后验概率。
3. 把这些概率作为软计数，累计缺少能力时的预期答对数和总次数。
4. 分别用 Beta 先验更新每个 `alpha_j` 的后验均值。
5. 重复直到收敛。

这不是 Infer.NET 的 expectation propagation 实现，但保留了第 2 章最重要的建模约束：guess probability 从未使用 ground-truth skills 直接估计。

## Ground truth

模拟数据内部确实存在隐藏 `truth`，它只用于生成答题和模拟自评误差，不出现在玩家可见的推断与评价逻辑中。

玩家看到并用于开发评价的是 `self`：匿名自评技能。它被故意加入少量错误，强调 approximate ground truth 并不等于绝对真值。

## 诊断

推断检查：从基础模型进行 ancestral-style sampling，先生成真实能力，再按“具备所需能力时 90%、缺少时 25%”生成答题；之后只给模型答题，看能否大体恢复生成时的能力。

模型检查：用开发样本的自评技能暂时固定 `skill`，计算每道题的模型预计答对比例，与真实答对比例并排展示。该步骤只用于诊断，不参与后续 `alpha_j` 学习。

## 评价

主要指标沿用书中的思路：

- Negative log probability：衡量模型给 ground-truth reference 的概率有多高，较低更好；对自信但错误的预测惩罚大。
- ROC AUC：衡量能力概率对具备/不具备能力的排序质量，对概率校准本身不敏感。

游戏另外展示 50% 阈值下一致率作为直观辅助，不把它当作唯一指标。
