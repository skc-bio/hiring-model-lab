# GAME DESIGN

## 玩家目标

玩家始终在解决同一个现实问题：以后面对一个新的招聘候选人，只看到 9 道选择题答案，怎样更可靠地推断三项潜在能力。

模型诊断、参数学习和模型比较都只是为这个目标服务，不单独构成新的游戏目标。

## 数据角色

历史匿名试测记录包含两类信息：

1. 9 道题的答对/答错：模型真正能够使用的输入。
2. 三项能力的匿名自评：开发期近似 ground truth，只用于评价和诊断。

240 条记录随机拆分为 160 条开发数据和 80 条最终独立检验数据。Holdout 在模型构建期间完全锁住。

## 流程（按书第 2 章的认知顺序）

### 1. 先说明 ground truth 从哪里来

玩家先理解：潜在能力本身不可直接观察，因此用匿名自评作为近似参考。游戏明确提醒自评可能有误，模型不能把它作为输入。

### 2. 构建简单模型

题目需要哪些能力由题目设计者提供。模型假设：

- 每项能力先简化成掌握/未掌握。
- 未看答案前，每项能力先验为 50%。
- 如果具备题目需要的全部能力，90% 概率答对。
- 如果缺少所需能力，仍可能答对。

玩家通过选择题自己得到四选一随机猜中为 25%。第一版模型就直接按 90% / 25% 这组直观规则工作，不把 25% 提前包装成待学习参数，也不提示后面会有逐题参数。

### 3. 先在小例子上测试

用 Q1/Q4/Q7 三道单能力题，让玩家先凭直觉判断答案模式，再看模型后验。目的不是教公式，而是确认模型推断方向符合常识。

### 4. 先看真实数据

玩家先看 160 × 9 的原始答题矩阵，观察人与题之间的结构，再运行模型。

### 5. 第一轮真实推断

基础模型直接用 90% / 25% 的规则推断三项能力。之后才把推断能力矩阵和自评参考矩阵放在一起检查。

### 6. 分清 bad data / bad model / bad inference

玩家选择如何排查。正确方向是：让模型自己生成一批能力已知的合成数据，再看推断能否把生成时的能力找回来。

如果合成数据上工作正常，而真实数据上仍有明显问题，就把注意力转向模型假设。

### 7. 用参考能力定位模型假设

在开发样本上暂时把自评能力当作已知，计算每道题在基础模型下应该有多少人答对，再和实际答对比例逐题比较。

玩家先观察“偏差因题而异”，然后自己回头选择最值得检查的规则：缺少所需能力时，每道题都按 25% 的答对概率处理。

这一步才出现关键认知：四选一随机猜中 25% 本身没错，但真实的人不一定完全随机作答；排除法、经验和题目线索会让不同题目的实际猜对概率不同。

### 8. 学习逐题 alpha

每道题增加自己的 `alpha_j`。所有参数使用同一个 `Beta(2.5, 7.5)` 先验，均值为 0.25。

学习 `alpha_j` 时不使用自评标签。模型同时根据答题数据推断能力和题目 guess probability。

### 9. 独立检验

两个模型冻结后才解锁 80 条 holdout。比较：

- 平均负对数概率：概率本身是否更接近参考能力；
- AUC：候选人的能力排序是否更好；
- 50% 阈值一致率：只作直观辅助。

### 10. 个体复盘

选出两种模型后验差异最大的 holdout 样本。玩家看到同一份答案如何因为题目 guess probability 不同而改变能力证据强度。

## 叙事原则

- 系统始终直接对玩家说“你现在为什么要做这一步”。
- 不在开场透露“共享 alpha vs 逐题 alpha”是最终比较。
- 不把 statistical jargon 当成解释；每个术语出现时必须先给具体含义。
- 发现问题依靠玩家先看证据、再做选择，而不是标题直接给结论。
- 自评标签、合成数据、开发数据和 holdout 的角色必须始终清楚区分。

## 结尾：成绩与留言

最终复盘后增加一个社区区块。玩家的互动成绩来自前面 9 个关键判断的首次作答过程，而不是模型在固定模拟数据上的 NLL/AUC（后者对所有玩家相同，不适合作为个人成绩）。首次判断正确计 100；收到反馈后修正计 60；最终取平均。

玩家可选择填写显示名、星级和留言。提交采用 GitHub Issue 草稿：游戏负责生成公开记录，玩家本人负责最终发布。公开排行榜和留言由 GitHub Actions 从仍然开放的 Issues 生成 `community.json`，页面只读取公开快照。
